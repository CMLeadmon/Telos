entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  websecure:
    address: ":443"
  turn:
    address: ":5349"

providers:
  docker:
    exposedByDefault: false
    network: telos-ingress
  file:
    directory: /etc/traefik/dynamic
    watch: true

log:
  level: DEBUG


