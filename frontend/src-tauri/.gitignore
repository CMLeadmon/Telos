# Rust build output. 4.6 GB after one cargo test run.
/target/

# Generated by tauri-build from tauri.conf.json and capabilities/ on every
# build. Regenerating is deterministic, so committing it only creates conflicts.
/gen/
