# Telos Clean-Node Installation Guide

This document describes the automated installation procedure for setting up Telos on a clean Linux host node.

## Prerequisites
- Linux kernel with `openat2` and `renameat2` support.
- Podman or Docker Compose runtime.
- Mode `0600` `.env` credentials file.
